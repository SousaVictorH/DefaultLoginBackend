const authConfig = require('./configs/auth.json');
const mongoConfig = require('./configs/mongodb');
const emailConfig = require('./configs/email-transporter-config');

module.exports = {
    authConfig,
    mongoConfig,
    emailConfig
};